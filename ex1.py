# Inicializa um vetor de tamanho 5
vetor = [0] * 5

# Pede ao usuário para preencher os elementos
for i in range(5):
    vetor[i] = input(f"Digite o elemento {i + 1}: ")

# Exibe o vetor preenchido
print("Vetor preenchido:", vetor)